from scipy.stats import chisquare

expected = [50, 50, 50, 50, 50] #список ожидаемых количество клиентов для каждого дня недели
observed = [48, 50, 47, 53, 60] #список содержит наблюдаемое количество клиентов в каждый день недели

chi2_start, p_value = chisquare(observed, expected)

print ("ХИ-квадрат статистика -", chi2_start)
print ("p-значение", p_value)

if p_value < 0,05:
print ("Отвергаем нулевую гипотезу, данные не согласуются с утверждением владельза компании")
else:
print ("Не отвергаем нулевую гипотезу, данные согласуются с утверждением владельза компании")
